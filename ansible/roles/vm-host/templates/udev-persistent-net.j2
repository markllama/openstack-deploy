#
# See if we can rename the alternate interfaces
#
{% for iface in item %}
# PCI device
SUBSYSTEM=="net", ACTION=="add", DRIVERS=="?*", \
  ATTR{address}=="{{ iface.value.mac }}",           \
  ATTR{type}=="1", NAME="if-{{ iface.key }}"
{% endfor %}
