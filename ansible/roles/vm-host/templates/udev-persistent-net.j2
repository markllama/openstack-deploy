#
# See if we can rename the alternate interfaces
#
{% for iface in bridges %}
# PCI device
SUBSYSTEM=="net", ACTION=="add", DRIVERS=="?*", \
  ATTR{address}=="{{ item.value.mac }}",           \
  ATTR{type}=="1", NAME="if-{{ item.key }}"
{% endfor %}
