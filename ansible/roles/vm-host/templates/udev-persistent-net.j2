#
# See if we can rename the alternate interfaces
#
{% for name, spec in dict_item.items() %}
# PCI device
SUBSYSTEM=="net", ACTION=="add", DRIVERS=="?*", \
  ATTR{address}=="{{ spec.mac }}",           \
  ATTR{type}=="1", NAME="if-{{ name }}"
{% endfor %}
